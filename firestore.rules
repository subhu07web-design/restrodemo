rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write on all collections for seamless operation
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
